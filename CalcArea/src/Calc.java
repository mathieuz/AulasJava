
public class Calc {
	
	double calcarea(double base, double alt) {
		
		if (this.alt == this.base) {
			return alt * alt;
			System.out.println("A área de um quadrado com base e altura = "+alt+" é "+alt*alt);
		}
		
	}
	
	double calcarea(double base, double alt) {
		return base * alt / 2;
	}
	
	double calcarea(double base, double alt) {
		return base * alt;
	}
	
}
